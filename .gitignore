__pycache__/
*.pyc
*.py
*.json.bak

# Datos generados — publicados a proposito desde 2026-09-13 (decision del
# usuario, incluye costo real). Otros *.json (configs, backups) se ignoran.
*.json
!data_d.json
!data_360.json

# Excels fuente (stock/costo interno) — nunca al repo publico
*.xlsx
*.xls
